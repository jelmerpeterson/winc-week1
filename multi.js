for (let multi = 1; multi <= 10; multi++) {
  for (let count = 1; count <= 10; count++) {
    let result = multi * count;
    console.log(`${count} * ${multi} = ${result}`);
  }
}

// eerst maak je een grote for loop waarin je aangeeft
// hoeveel keer een grote loop moet plaats vinden
// soort parent element, vervolgens maak je een child for loop
// Daarin maak je een volledig nieuwe loop
//
